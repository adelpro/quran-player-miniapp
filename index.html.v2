<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>🎵 Quran Player</title>
  <style>
    :root {
      /* Auto-detect system preference */
      @media (prefers-color-scheme: dark) {
        --bg-primary: #121212;
        --bg-card: #1E1E1E;
        --bg-card-hover: #2C2C2C;
        --text-primary: #EEEEEE;
        --text-secondary: #B0B0B0;
        --text-muted: #757575;
        --border-color: #333333;
        --accent: #9E9E9E;
        --accent-hover: #C0C0C0;
        --shadow: 0 2px 8px rgba(0,0,0,0.30);
        --shadow-hover: 0 4px 16px rgba(0,0,0,0.40);
      }

      @media (prefers-color-scheme: light) {
        --bg-primary: #FAFAFA;
        --bg-card: #FFFFFF;
        --bg-card-hover: #F5F5F5;
        --text-primary: #212121;
        --text-secondary: #616161;
        --text-muted: #9E9E9E;
        --border-color: #E0E0E0;
        --accent: #616161;
        --accent-hover: #424242;
        --shadow: 0 2px 8px rgba(0,0,0,0.06);
        --shadow-hover: 0 4px 16px rgba(0,0,0,0.10);
      }

      /* Default (light) */
      --bg-primary: #FAFAFA;
      --bg-card: #FFFFFF;
      --bg-card-hover: #F5F5F5;
      --text-primary: #212121;
      --text-secondary: #616161;
      --text-muted: #9E9E9E;
      --border-color: #E0E0E0;
      --accent: #616161;
      --accent-hover: #424242;
      --shadow: 0 2px 8px rgba(0,0,0,0.06);
      --shadow-hover: 0 4px 16px rgba(0,0,0,0.10);
    }

    * { margin: 0; padding: 0; box-sizing: border-box; }

    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      background: var(--bg-primary);
      color: var(--text-primary);
      min-height: 100vh;
      transition: background 0.3s, color 0.3s;
    }

    .container {
      max-width: 400px;
      margin: 0 auto;
      padding: 16px;
      padding-bottom: 120px;
    }

    /* Header */
    .header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 12px 0;
      margin-bottom: 20px;
    }

    .header h1 {
      font-size: 18px;
      font-weight: 600;
    }

    /* Cards */
    .card {
      background: var(--bg-card);
      border-radius: 12px;
      padding: 16px;
      margin-bottom: 12px;
      box-shadow: var(--shadow);
      border: 1px solid var(--border-color);
      transition: all 0.2s;
    }

    .section-title {
      font-size: 12px;
      font-weight: 600;
      color: var(--text-muted);
      text-transform: uppercase;
      letter-spacing: 0.5px;
      margin-bottom: 12px;
    }

    /* Sources - Simple tabs */
    .sources {
      display: flex;
      gap: 8px;
      overflow-x: auto;
      padding-bottom: 4px;
    }

    .source-tab {
      padding: 8px 14px;
      border-radius: 20px;
      border: 1px solid var(--border-color);
      background: var(--bg-card);
      color: var(--text-secondary);
      font-size: 13px;
      white-space: nowrap;
      cursor: pointer;
      transition: all 0.2s;
    }

    .source-tab.active {
      background: var(--text-primary);
      color: var(--bg-card);
      border-color: var(--text-primary);
    }

    /* Search */
    .search-box {
      position: relative;
    }

    .search-input {
      width: 100%;
      padding: 12px 16px;
      padding-right: 44px;
      border: 1px solid var(--border-color);
      border-radius: 10px;
      background: var(--bg-primary);
      color: var(--text-primary);
      font-size: 15px;
      outline: none;
    }

    .search-input:focus {
      border-color: var(--accent);
    }

    .search-icon {
      position: absolute;
      right: 14px;
      top: 50%;
      transform: translateY(-50%);
      color: var(--text-muted);
    }

    /* Reciter - Single Select (Radio) */
    .reciter {
      display: flex;
      align-items: center;
      padding: 10px;
      margin: 4px 0;
      border-radius: 8px;
      cursor: pointer;
      transition: background 0.2s;
    }

    .reciter:hover {
      background: var(--bg-card-hover);
    }

    .reciter.selected {
      background: var(--bg-card-hover);
      border: 1px solid var(--accent);
    }

    .reciter-radio {
      width: 18px;
      height: 18px;
      border-radius: 50%;
      border: 2px solid var(--border-color);
      margin-left: 12px;
      display: flex;
      align-items: center;
      justify-content: center;
      transition: all 0.2s;
    }

    .reciter.selected .reciter-radio {
      border-color: var(--accent);
      background: var(--accent);
    }

    .reciter.selected .reciter-radio::after {
      content: '';
      width: 8px;
      height: 8px;
      border-radius: 50%;
      background: var(--bg-card);
    }

    .reciter-info {
      flex: 1;
    }

    .reciter-name {
      font-size: 15px;
      font-weight: 500;
    }

    .reciter-riwayah {
      font-size: 12px;
      color: var(--text-muted);
    }

    /* Player - Fixed Bottom */
    .player {
      position: fixed;
      bottom: 0;
      left: 50%;
      transform: translateX(-50%);
      width: 100%;
      max-width: 400px;
      background: var(--bg-card);
      border-top: 1px solid var(--border-color);
      padding: 12px 16px;
      box-shadow: 0 -4px 20px rgba(0,0,0,0.10);
      z-index: 100;
    }

    .player-content {
      display: flex;
      align-items: center;
      gap: 12px;
    }

    .player-avatar {
      width: 44px;
      height: 44px;
      border-radius: 50%;
      background: linear-gradient(135deg, var(--accent), var(--accent-hover));
      display: flex;
      align-items: center;
      justify-content: center;
      color: var(--bg-card);
      font-weight: 600;
      font-size: 14px;
    }

    .player-info {
      flex: 1;
      min-width: 0;
    }

    .player-title {
      font-weight: 600;
      font-size: 14px;
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
    }

    .player-artist {
      font-size: 12px;
      color: var(--text-secondary);
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
    }

    .player-controls {
      display: flex;
      align-items: center;
      gap: 4px;
    }

    .player-btn {
      width: 36px;
      height: 36px;
      border-radius: 50%;
      border: none;
      background: transparent;
      color: var(--text-primary);
      cursor: pointer;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 16px;
      transition: background 0.2s;
    }

    .player-btn:hover {
      background: var(--bg-card-hover);
    }

    .player-btn.play {
      background: var(--text-primary);
      color: var(--bg-card);
    }

    .player-btn.play:hover {
      background: var(--accent);
    }

    .progress {
      margin-top: 10px;
    }

    .progress-bar {
      width: 100%;
      height: 4px;
      background: var(--border-color);
      border-radius: 2px;
      overflow: hidden;
    }

    .progress-fill {
      width: 35%;
      height: 100%;
      background: var(--accent);
      border-radius: 2px;
    }

    .progress-time {
      display: flex;
      justify-content: space-between;
      font-size: 11px;
      color: var(--text-muted);
      margin-top: 4px;
    }
  </style>
</head>
<body>
  <div class="container">
    <!-- Header -->
    <div class="header">
      <h1>🎵 مشغل القرآن</h1>
    </div>

    <!-- Sources -->
    <div class="card">
      <div class="section-title">📡 المصادر</div>
      <div class="sources">
        <div class="source-tab active">itqan CMS</div>
        <div class="source-tab">mp3quran</div>
        <div class="source-tab">EveryAyah</div>
        <div class="source-tab">Quran.com</div>
      </div>
    </div>

    <!-- Search -->
    <div class="card search-box">
      <input type="text" class="search-input" placeholder="🔍 البحث عن قارئ...">
      <span class="search-icon">🔍</span>
    </div>

    <!-- Reciters - Single Select -->
    <div class="card">
      <div class="section-title">📿 القارئ</div>
      <div class="reciter selected" onclick="selectReciter(this)">
        <div class="reciter-radio"></div>
        <div class="reciter-info">
          <div class="reciter-name">بدر عبد الرحمن</div>
          <div class="reciter-riwayah">رواية عاصم</div>
        </div>
      </div>
      <div class="reciter" onclick="selectReciter(this)">
        <div class="reciter-radio"></div>
        <div class="reciter-info">
          <div class="reciter-name">مجد الزامل</div>
          <div class="reciter-riwayah">رواية حفص</div>
        </div>
      </div>
      <div class="reciter" onclick="selectReciter(this)">
        <div class="reciter-radio"></div>
        <div class="reciter-info">
          <div class="reciter-name">عبد الرحمن السديس</div>
          <div class="reciter-riwayah">رواية شعبة</div>
        </div>
      </div>
      <div class="reciter" onclick="selectReciter(this)">
        <div class="reciter-radio"></div>
        <div class="reciter-info">
          <div class="reciter-name">الشريم أبو بكر</div>
          <div class="reciter-riwayah">رواية قالون</div>
        </div>
      </div>
    </div>
  </div>

  <!-- Player -->
  <div class="player">
    <div class="player-content">
      <div class="player-avatar">ب ع</div>
      <div class="player-info">
        <div class="player-title">سورة الفاتحة</div>
        <div class="player-artist">بدر عبد الرحمن • رواية عاصم</div>
      </div>
      <div class="player-controls">
        <button class="player-btn">🔀</button>
        <button class="player-btn play">▶️</button>
        <button class="player-btn">🔁</button>
      </div>
    </div>
    <div class="progress">
      <div class="progress-bar">
        <div class="progress-fill"></div>
      </div>
      <div class="progress-time">
        <span>0:00</span>
        <span>0:50</span>
      </div>
    </div>
  </div>

  <script>
    // Auto-detect system theme
    function updateTheme() {
      const dark = window.matchMedia('(prefers-color-scheme: dark)').matches;
      document.documentElement.style.setProperty('--bg-primary', dark ? '#121212' : '#FAFAFA');
      document.documentElement.style.setProperty('--bg-card', dark ? '#1E1E1E' : '#FFFFFF');
      document.documentElement.style.setProperty('--bg-card-hover', dark ? '#2C2C2C' : '#F5F5F5');
      document.documentElement.style.setProperty('--text-primary', dark ? '#EEEEEE' : '#212121');
      document.documentElement.style.setProperty('--text-secondary', dark ? '#B0B0B0' : '#616161');
      document.documentElement.style.setProperty('--text-muted', dark ? '#757575' : '#9E9E9E');
      document.documentElement.style.setProperty('--border-color', dark ? '#333333' : '#E0E0E0');
      document.documentElement.style.setProperty('--accent', dark ? '#9E9E9E' : '#616161');
      document.documentElement.style.setProperty('--accent-hover', dark ? '#C0C0C0' : '#424242');
      document.documentElement.style.setProperty('--shadow', dark ? '0 2px 8px rgba(0,0,0,0.30)' : '0 2px 8px rgba(0,0,0,0.06)');
    }

    // Listen for system theme changes
    window.matchMedia('(prefers-color-scheme: dark)').addEventListener('change', updateTheme);
    updateTheme();

    // Single reciter select
    function selectReciter(el) {
      document.querySelectorAll('.reciter').forEach(r => r.classList.remove('selected'));
      el.classList.add('selected');
    }

    // Source tabs
    document.querySelectorAll('.source-tab').forEach(tab => {
      tab.addEventListener('click', () => {
        document.querySelectorAll('.source-tab').forEach(t => t.classList.remove('active'));
        tab.classList.add('active');
      });
    });
  </script>
</body>
</html>
